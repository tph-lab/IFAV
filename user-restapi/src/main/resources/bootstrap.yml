spring:
  application:
    name: ifav-provider-reg
eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:a@localhost:7301/eureka
  instance:
    preferIpAddress: true
    instance-id: ifav-provider-reg_1
    lease-expiration-duration-in-seconds: 90
    lease-renewal-interval-in-seconds: 30